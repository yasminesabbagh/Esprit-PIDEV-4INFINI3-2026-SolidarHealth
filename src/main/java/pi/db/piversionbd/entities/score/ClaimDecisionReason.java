package pi.db.piversionbd.entities.score;

public enum ClaimDecisionReason {
    AUTO_APPROVAL,
    LOW_SCORE,
    EXCLUDED_CONDITION,
    FRAUD_SUSPECTED,
    PACKAGE_LIMIT_EXCEEDED,
    INVALID_DOCUMENTS,
    MANUAL_APPROVAL,
    MANUAL_REJECTION
}
